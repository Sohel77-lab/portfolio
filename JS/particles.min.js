// Particles.js configuration
particlesJS("particles-js", {
  particles: {
    number: {
      value: 120,  // Number of particles
      density: {
        enable: true,
        value_area: 800
      }
    },
    color: {
      value: "#ff9f00"  // Particle color (Orange)
    },
    shape: {
      type: "circle",  // Particle shape
    },
    opacity: {
      value: 0.7,  // Particle opacity
      random: true,
      anim: {
        enable: true,
        speed: 1,
        opacity_min: 0.1,
        sync: false
      }
    },
    size: {
      value: 6,  // Particle size
      random: true,
      anim: {
        enable: true,
        speed: 3,
        size_min: 0.1,
        sync: false
      }
    },
    line_linked: {
      enable: true,
      distance: 120,  // Distance between lines
      color: "#ff9f00",  // Line color (Orange)
      opacity: 0.4,  // Line opacity
      width: 2  // Line width
    },
    move: {
      enable: true,
      speed: 4,  // Particle speed
      direction: "none",
      random: true,
      straight: false,
      out_mode: "out",
      bounce: false
    }
  },
  interactivity: {
    events: {
      onhover: {
        enable: true,
        mode: "repulse"  // Particles repulse when mouse hovers
      },
      onclick: {
        enable: true,
        mode: "push"  // More particles push out on mouse click
      }
    },
    modes: {
      repulse: {
        distance: 100,
        duration: 0.4
      },
      push: {
        particles_nb: 6
      }
    }
  },
  retina_detect: true
});
